<nav aria-label="{{ 'Select page'|trans }}">
  <ul class="pagination">
    {% for i in range(1, 10) %}
      <li class="page-item {% if i == page %}active{% endif %}"><a href="/{{ i }}" class="page-link">{{ i }}</a></li>
    {% endfor %}
  </ul>
</nav>
